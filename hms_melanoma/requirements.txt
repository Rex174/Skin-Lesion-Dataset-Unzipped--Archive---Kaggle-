# ===================================================================================
# FYP - PHASE 2: HMS Backend  |  TP070818 | Ramaneiss Pillai S Gopalan
# requirements.txt
#
# Install all dependencies:
#   pip install -r requirements.txt
# ===================================================================================

# Core Flask
flask==3.0.3
flask-sqlalchemy==3.1.1
werkzeug==3.0.3

# Database
# SQLite is built into Python — no extra install needed for development.
# For PostgreSQL (production), uncomment below:
# psycopg2-binary==2.9.9

# PDF report generation
reportlab==4.2.2

# Image processing (for predict.py preprocessing)
Pillow==10.4.0

# ML inference — models were saved with Keras 3.13.2 (TF backend).
# Must use tensorflow==2.19.0 + keras==3.13.2 to match the training environment.
# Use standalone `import keras` NOT `import tensorflow as tf; tf.keras` (removed in TF 2.16+).
tensorflow==2.19.0
keras==3.13.2
numpy==1.26.4

# Utilities
python-dotenv==1.0.1
